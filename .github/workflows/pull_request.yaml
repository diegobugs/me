name: "Validate PRs"

on:
  pull_request:
    types: [opened, synchronize]
    branches: ["main"]
jobs:
  validate_develop_usage:
    runs-on: ubuntu-latest
    steps:
      - name: "Validate feature is from develop"
        if: ${{ !startsWith(github.event.pull_request.head.ref, 'develop') || !startsWith(github.event.pull_request.head.ref, 'hotfix') }}
        run: echo "is $BRANCH" && exit 1
